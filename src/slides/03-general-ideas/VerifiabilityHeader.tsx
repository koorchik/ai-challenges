import { SectionHeader } from '../../components/SectionHeader';

export default function VerifiabilityHeader() {
  return (
    <SectionHeader
      title="Головне специфікація"
      subtitle="Головне ТЗ й ШІ все напише сам?"
    />
  );
}
